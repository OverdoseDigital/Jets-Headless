{%- comment -%}
  This snippet contains the two standard Okendo style tags (see below),
  that are included in this special way so that they are not render-blocking
  during page load.

  {{ shop.metafields.okendo.ReviewsWidgetStyleTag }}
  {{ shop.metafields.okendo.StyleCustomizations }}

{%- endcomment -%}

{% render 'okendo-preview-head' %}